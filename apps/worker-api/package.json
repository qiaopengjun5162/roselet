{
  "name": "@roselet/worker-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "tsc --outDir .tmp-test --module NodeNext --moduleResolution NodeNext --target ES2022 src/rose.ts src/auth.ts && node --test src/*.test.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.22.0"
  }
}
