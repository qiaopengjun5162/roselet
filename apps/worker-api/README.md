# @roselet/worker-api

> **历史方案说明**
>
> 当前 Roselet 生产环境已切换为 `Vercel Web + AWS Lightsail Rust backend + Docker Postgres + Caddy HTTPS`。
> `apps/worker-api` 是早期 Cloudflare Workers 迁移试点的代码骨架，仅作为历史参考保留在仓库中，不再维护，也不参与当前 CI/CD。
>
> 当前生产部署文档：[docs/AWS_LIGHTSAIL_DEPLOYMENT.md](../docs/AWS_LIGHTSAIL_DEPLOYMENT.md)
